# Railway 部署配置：安装 ffmpeg 用于音视频合并
[phases.setup]
nixPkgs = ["python312", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "sh -c 'gunicorn -w 1 --threads 8 -b 0.0.0.0:$PORT --timeout 600 app:app'"
